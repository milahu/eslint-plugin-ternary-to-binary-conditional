{
  "name": "eslint-plugin-ternary-to-binary-conditional",
  "version": "0.0.1",
  "description": "transform (t ? c : false) to (t && c)",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "milahu@gmail.com",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha tests --recursive"
  },
  "dependencies": {
    "requireindex": "^1.2.0"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "mocha": "^7.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "CC-0"
}
